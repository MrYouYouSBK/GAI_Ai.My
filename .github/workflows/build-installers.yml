name: Build installers

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release-safety:
    name: Release safety check
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Reject tracked local credentials
        run: npm run check:release-safety

  unit-tests:
    name: Offline and configuration tests
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci --ignore-scripts

      - name: Capture dependency security baseline
        shell: bash
        run: |
          npm audit --json > npm-audit.json || true
          npm run audit:security:report -- npm-audit.json security-audit-summary.md

      - name: Syntax-check Project S trust surfaces
        run: |
          node --check src/capabilities/permission-center.js
          node --check src/capabilities/action-receipt.js
          node --check src/capabilities/tool-audit.js
          node --check src/api/routes/settings.js
          node --check src/ui/brain-ui/gai-control-center.js
          node --check src/ui/brain-ui/app.js
          node --check src/ui/brain-ui/voice-core.js
          node --check src/ui/brain-ui/voice-panel.js
          node --check src/ui/brain-ui/voice-wake.js
          node --check src/ui/brain-ui/voice-continuous.js
          node --check src/ui/brain-ui/tts-fx.js
          node --check src/ui/brain-ui/legacy-compat.js
          node --check src/compat/legacy-bailongma.js
          node --check electron/legacy-compat.cjs
          node --check electron/main.cjs

      - name: Verify GAI AI 3.3 runtime
        run: |
          npm run test:offline
          npm run test:config-upgrade
          npm run test:voice-continuous
          npm run test:gai-v31
          npm run test:gai-v32
          npm run test:startup-task-briefing
          npm run test:local-ai
          npm run test:community-updater
          npm run test:mac-signing-key-bundle
          npm run test:websocket-security
          npm run test:project-s-trust

      - name: Upload dependency security baseline
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: GAI-AI-dependency-security-audit
          path: |
            npm-audit.json
            security-audit-summary.md
          if-no-files-found: warn

  windows:
    name: Windows x64 installer
    needs:
      - release-safety
      - unit-tests
    runs-on: windows-2022
    env:
      CSC_IDENTITY_AUTO_DISCOVERY: 'false'
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build Windows installer
        run: npm run build:win

      - name: Create Windows checksums
        shell: pwsh
        run: |
          $lines = Get-ChildItem dist/*.exe | ForEach-Object {
            $hash = (Get-FileHash $_.FullName -Algorithm SHA256).Hash.ToLower()
            "$hash  $($_.Name)"
          }
          $lines | Set-Content -Encoding utf8 dist/SHA256SUMS-windows.txt

      - name: Upload Windows installer
        uses: actions/upload-artifact@v4
        with:
          name: GAI-AI-windows-x64
          path: |
            dist/*.exe
            dist/*.exe.blockmap
            dist/latest.yml
            dist/SHA256SUMS-windows.txt
          if-no-files-found: error

  macos:
    name: macOS ${{ matrix.arch }} trusted DMG
    needs:
      - release-safety
      - unit-tests
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: x64
            runner: macos-15-intel
          - arch: arm64
            runner: macos-15
    runs-on: ${{ matrix.runner }}
    env:
      GAI_REQUIRE_MAC_SIGNING: ${{ (github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v')) && github.event_name != 'pull_request' && 'true' || 'false' }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Prepare Apple trust credentials for publishable builds
        if: env.GAI_REQUIRE_MAC_SIGNING == 'true'
        env:
          GAI_MAC_SIGNING_KEY_BUNDLE: ${{ secrets.KEY }}
          MAC_CERTIFICATE_BASE64: ${{ secrets.MAC_CERTIFICATE_BASE64 }}
          MAC_CERTIFICATE_PASSWORD: ${{ secrets.MAC_CERTIFICATE_PASSWORD }}
          APPLE_API_KEY_P8: ${{ secrets.APPLE_API_KEY_P8 }}
          APPLE_API_KEY_ID: ${{ secrets.APPLE_API_KEY_ID }}
          APPLE_API_ISSUER_ID: ${{ secrets.APPLE_API_ISSUER_ID }}
        run: node scripts/prepare-macos-signing-env.mjs

      - name: Disable signing only for non-publishable PR smoke builds
        if: env.GAI_REQUIRE_MAC_SIGNING != 'true'
        run: echo "CSC_IDENTITY_AUTO_DISCOVERY=false" >> "$GITHUB_ENV"

      - name: Install dependencies
        run: npm ci

      - name: Build macOS DMG
        run: npm run build:mac:${{ matrix.arch }}

      - name: Notarize and staple publishable DMG
        if: env.GAI_REQUIRE_MAC_SIGNING == 'true'
        run: npm run notarize:mac -- ${{ matrix.arch }}

      - name: Verify architecture, Developer ID, Gatekeeper and launch
        run: npm run verify:mac -- ${{ matrix.arch }}

      - name: Preserve architecture-specific update metadata
        run: mv dist/latest-mac.yml dist/latest-${{ matrix.arch }}-mac.yml

      - name: Create macOS checksum
        run: shasum -a 256 dist/GAI-AI-*-mac-${{ matrix.arch }}.dmg > dist/SHA256SUMS-macos-${{ matrix.arch }}.txt

      - name: Upload macOS DMG
        uses: actions/upload-artifact@v4
        with:
          name: GAI-AI-macos-${{ matrix.arch }}
          path: |
            dist/GAI-AI-*-mac-${{ matrix.arch }}.dmg
            dist/GAI-AI-*-mac-${{ matrix.arch }}.dmg.blockmap
            dist/GAI-AI-*-mac-${{ matrix.arch }}.zip
            dist/GAI-AI-*-mac-${{ matrix.arch }}.zip.blockmap
            dist/latest-${{ matrix.arch }}-mac.yml
            dist/SHA256SUMS-macos-${{ matrix.arch }}.txt
          if-no-files-found: error

  release:
    name: Publish stable GitHub Release
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
    needs:
      - windows
      - macos
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Validate stable release tag
        id: version
        shell: bash
        run: |
          set -euo pipefail
          version=$(node -p "JSON.parse(require('fs').readFileSync('package.json', 'utf8')).version")
          expected="v${version}"
          actual="${GITHUB_REF_NAME}"
          if [[ "$actual" != "$expected" ]]; then
            echo "::error::Stable tag $actual does not match package.json version $version (expected $expected)."
            exit 1
          fi
          echo "tag=$actual" >> "$GITHUB_OUTPUT"

      - name: Download installers
        uses: actions/download-artifact@v4
        with:
          path: release-assets
          merge-multiple: true

      - name: Create combined checksums
        working-directory: release-assets
        run: sha256sum *.exe *.dmg *.zip > SHA256SUMS.txt

      - name: Create or update release
        shell: bash
        run: |
          release_tag="${{ steps.version.outputs.tag }}"
          if gh release view "$release_tag" --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
            gh release upload "$release_tag" release-assets/* --clobber --repo "$GITHUB_REPOSITORY"
            gh release edit "$release_tag" --notes-file RELEASE_NOTES_3.3.md --title "GAI AI ${GITHUB_REF_NAME#v}" --repo "$GITHUB_REPOSITORY"
          else
            gh release create "$release_tag" release-assets/* \
              --repo "$GITHUB_REPOSITORY" \
              --target "$GITHUB_SHA" \
              --notes-file RELEASE_NOTES_3.3.md \
              --title "GAI AI ${GITHUB_REF_NAME#v}"
          fi
